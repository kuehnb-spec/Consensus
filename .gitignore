# macOS
.DS_Store

# Python virtual environment
.venv/

# Codex output
.codex-output/

# Xcode
build/
DerivedData/
*.xcuserstate
*.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/

# Swift Package Manager
.build/
.swiftpm/

# Benchmark fixtures — real phone-call audio, rasterized transcripts, and
# hand-corrected ground-truth files. Local-only; never committed because
# they contain real names and other PII from actual calls.
TestAudio/

# Python bytecode
__pycache__/
*.pyc

# Large model files — see source-index.md and ComputerMaintenance Task #6
# (Consensus model audit) for the cleanup plan
*.gguf
*.safetensors
*.bin
*.pt
*.ckpt
