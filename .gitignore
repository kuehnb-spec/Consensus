# macOS
.DS_Store

# Python virtual environment
.venv/

# Codex output
.codex-output/

# Xcode
build/
DerivedData/
*.xcuserstate
*.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/

# Swift Package Manager
.build/
.swiftpm/

# Stray swiftc droppings from direct compiler invocations
*.o
*.d
*.dia
*.swiftdeps

# Built app bundles (build with TranscriboApp/build-app.sh; ship via GitHub Releases)
*.app/

# local-assets -> ~/ProjectAssets/<project> (NAS-synced bulk files; never in git)
local-assets

# Benchmark fixtures — real phone-call audio, rasterized transcripts, and
# hand-corrected ground-truth files. Local-only; never committed because
# they contain real names and other PII from actual calls.
TestAudio/

# Python bytecode
__pycache__/
*.pyc

# Large model files — see source-index.md and ComputerMaintenance Task #6
# (Consensus model audit) for the cleanup plan
*.gguf
*.safetensors
*.bin
*.pt
*.ckpt

# Release artifacts (published to GitHub Releases, not tracked)
dist/
