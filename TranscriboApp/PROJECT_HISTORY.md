
Post-push, the rebase surfaced `HANDOFF-GUI-REVAMP.md` — a same-day handoff memo from another session. Reconciled it against the morning's work: its "GUI needs an .xcodeproj" blocker was actually the case-collision (now fixed and verified — the debug GUI binary launches and stays alive on the Studio), and its Task A hunks turned out not to exist on `wip/studio`; main already carried the live-path empty-pass guards, so only the legacy `TranscriptionProject.hasTranscript` fix needed porting (now requires non-empty segments). Annotated both handoff sections with corrections. Honored the handoff's identity guardrail in Quick Take: auto-applied intro-scan names now stay unconfirmed and render with an amber unverified marker until a human renames (renaming confirms). Finished the Phase 0 rename leftovers in README (title, layout, dead BDK-Transcribo paths). Task B — the FluidAudio vendored-vs-0.15.x decision plus CLI re-validation — is logged as open work.
