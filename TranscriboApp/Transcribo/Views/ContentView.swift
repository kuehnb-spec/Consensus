import SwiftUI

// The app's single UI surface. The pre-rewrite view tree that used to live in
// this file is archived at Brainstorming/archive/legacy-ui-2026-07/.
struct ContentView: View {
    var body: some View {
        RewrittenSurface()
    }
}
